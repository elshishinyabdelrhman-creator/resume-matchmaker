/**
 * Central export for app-wide TypeScript types.
 * Prefer importing from here in feature code for stable paths.
 */

export type { Database, Json } from "@/types/database";
export type { Application, Job, Resume, ApplicationRow, JobRow, ResumeRow } from "@/types/domain";
